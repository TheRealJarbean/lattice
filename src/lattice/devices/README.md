The interface/worker architecture may seem redundant, but it abstracts the thread and signal management for the higher level UI logic; which is *very* helpful and makes the UI code a lot cleaner. Qt in Python also has some unintuitive quirks that can cause connections to execute in the wrong thread (such as when using lambdas), the interface class enforces thread safety and proper execution of methods on a worker thread.