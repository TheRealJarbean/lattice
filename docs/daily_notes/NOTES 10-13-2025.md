- Change source button to safety settings add max temp
- Add copy/paste for recipe rows

# Eurotherm 2404 Config access instructions
Access -> pass 1 -> config -> pass 1 -> change settings (comms under HA) -> Ei, b confirm -> back to control

- Might need to move source comms to new thread min wait time 10ms per source because Windows
- Add tolerance as source attribute
- Reenable shutter disable controls

# Pressure Commands
- Read: #0002{Gauge}\r\n where Gauge is in [T1, I1, I2, I3]
- Off: #0030{Gauge}\r\n
- On: #0031{Gauge}\r\n